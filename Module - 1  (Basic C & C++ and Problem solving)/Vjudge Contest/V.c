#include<stdio.h>
int main()
{
    while(1) {
        int h, w;
        scanf("%d %d", &h, &w);
        if(h == 0 && w == 0) {
            break;
        }
        for(i = 1; i <= h; i++) {
            if(i % 2 == 1) {
                for()
            }
        }
    }
}
